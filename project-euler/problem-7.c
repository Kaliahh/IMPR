# include <stdio.h>
# include <math.h>


int main(void) {
  int a, c;
  double i, b;

  for(i = 1; i <= 20; i++) {
    b = sqrt(i);
    if (b != int) {
      a = i;

    }

    printf("i: %7f, a: %7d\n", i, a);



  }

  return 0;
}
