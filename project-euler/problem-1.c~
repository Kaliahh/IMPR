# include <stdio.h>

int main(void) {

  int a = 0, i = 0;
  
  for(i = 1; i < 1000; ++i) {
    if (i % 5 == 0) {
      a += i;
    }
    else if (i % 3 == 0) {
      a += i;
    }
    else
      ;
    printf("a: %d, i: %d\n",a, i);
  }

  printf("%d\n", a);


  return 0; 
}
