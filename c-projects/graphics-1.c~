# include <graphics.h>

int main(void) {

  int bigX, bigY;

  bigX = 640;
  bigY = 480;
  initwindow(bigX, bigY, "This is a window - press a key to close");

  lin(0, 0, bigX, bigY);
  setcolor(BLUE);

  line(bigX, 0, 0, bigY);

  getch();
  closegraph();

  return 0;
}
